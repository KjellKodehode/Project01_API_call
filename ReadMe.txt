REACT_ADVANCED API CALL

Make a fetch function and store the data in a useState, 
put that function inside a useEffect to fire the fetch function when page renders. 

console.log the data from api call, 
 
Optional: conditionally render the content from the api call. Remember to also include what should happen if something goes wrong. 

DELIVER with github pages link. 